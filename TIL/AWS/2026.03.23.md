# [2026-03-20]

## ☀️ 오늘의 목표 (Scrum)

- [Auto Scaling Group]
- [Route 53]
- [S3 버킷을 활용한 Web 배포]

## 📝 배운 내용 (Today I Learned)

### Custom AMI vs Launch Template
- Custom AMI : 인스턴스 유형이나 Security Group 설정이 포함되어있지 않다. OS 및 Package, Daemon만 설치되어 있다.
- Launch Template : Custom AMI + Security Group, Key-Pair, Subnet, IAM Role, EBS 설정 등과 같이 생성될 때 필요한 옵션들과 함께 묶여있다.
  > Custom AMI 정보에 추가적으로 '인스턴스를 생성할때 설정했던 내용'들을 미리 정의하기 위한 목적으로 생각하자.


<img width="1100" height="395" alt="image" src="https://github.com/user-attachments/assets/aaf11282-6585-442a-a11a-0d0764a79c20" />


- Auto-Scaling을 통해 Instance가 생성될 때마다 추가 설정을 일일이 지정해야하는데 현실적으로 불가능함... 그래서 Launch Template을 활용한다.


### Auto-Scaling Group 설정하기
- 주요 목적 : 고가용성(High Availability).
- Scale-Out : 서버 갯수를 늘린다. / Scale-In : 서버 갯수를 줄인다.
- Scale-Up : 서버 Spec(CPU, Memory 등등)을 높인다. / Scale-down : 서버 Spec을 낮춘다.
- 구동중인 서버를 건들지 않으려면 Scale In/Out 밖에 없다. 그렇기 때문에 Auto-Scaling Group에서는 이 방식을 채택한다.
- 구성 순서
  1. Elastic Load Balancer 생성
     - "Health Checks" → "Elastic Load Balancer Check(Recommended)"를 활성화!!
     - "Health Check Grace(유예) Period" → Health Check를 언제부터 시작할지 정하는 설정이다. 부팅 시간을 보상하기 위한 옵션.
  2. Target Group 생성 - Target을 비워둔 채로 생성!!
  3. Auto-Scaling Group 생성하는데, 이때 만들어둔 Target Group을 연동한다!!
     - "Auto-Scaling" → "Target Tracking Scaling Policy" 설정 활성화
     - "Monitoring" → "Enable group metrics collection within CloudWatch" 설정 활성화 (인스턴스의 지표를 CloudWatch에서 보겠다.)


- Auto-Scaling Group을 지우면, 그에 수반되는 ***Resource(EC2 포함!!!)들도 같이 삭제***된다.


### Route 53
- AWS가 제공하는 DNS(Port 번호 : 53) 서비스.
- AWS내에 존재하는 다양한 서버나 엔드포인트들(EC2, ELB, Cloudfront, S3, API-GW, Lambda 등등...)로의 라우팅 기능 제공.


1. 미리 가지고 있는 Domain 이름에 맞춰 Hosting Zone을 생성 
2. Route 53 에서 제공하는 NS(Name Server)를 DNS 호스팅 업체에 등록
   - 도메인의 실제 주인은 호스팅 업체.
   - AWS에서 Hosting Zone 생성 시 만들어진 Name Server들을 호스팅 업체에 알려준다.
     > 나의 Zone에 대한 정보를 담고 있는 것들을 알려준다는 의미!


   - 호스팅 업체는 DNS 서버 상 상위 서버로 해당 정보를 등록시켜준다.
3. 누군가 상위 서버에 내 Zone(jaehyunkim.cloud)에 대한 질의를 하면 AWS의 Name Server 주소를 알려준다!
 

### S3
- Simple Storage Service.
- Static Web Site Hosting 기능을 제공하며, 이를 가지고 Route 53의 글로벌 주소로 연결시켜줄 수 있다.
  > 이렇게되면 마치 WEB 서버처럼 배포 가능하다.


1. S3 Bucket 생성 시, ***"Global namespace"로 설정해야 Route 53 연동 가능!***
2. 생성 완료 후, "Properties" → "Static Web Site Hosting - Enable" 설정
   - 하지만, 이렇게해도 접속해보면 ***Permission Denied*** 발생한다.
3. ***Bucket Policy*** 등록
   - JSON 형태, 직접 양식에 맞춰 코딩하거나, AWS에서 제공하는 Policy Generator를 활용하면 된다.
4. Route 53의 "Create Records" → "Alias" → "S3 Website Endpoint" 설정
   > 꼭 WEB 배포만 있는게 아니고, 다른 것들도 만들 수 있다. 참고하자!
   
   
## 🔥 도전 과제 & 해결 (Challenge & Solution)

- **과제/문제**: X
- **시도 및 해결**: X

## 💭 오늘의 회고 (Retrospective)

- **Keep (좋았던 점)**: 뒤죽박죽이던 것들이 이제는 명확해지고 있다.
- **Problem (아쉬웠던 점)**: DNS 레코드 종류에 대한 공부가 필요하다.
- **Try (시도할 점)**: CCNA 강의 저녁에 계속 들어야한다.

## 🔗 참고 자료 (References)
