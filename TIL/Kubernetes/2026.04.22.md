# [2026-04-22]

## ☀️ 오늘의 목표 (Scrum)

- [HPA를 통한 Auto-Scaling 구현]
- [HAproxy를 통한 삼중화 기법]
- [K8s의 Node 선택 기법]
- [Helm] 


## 📝 배운 내용 (Today I Learned)

## 1. HPA(Horizontal Pod Auto-Scaler)
- 말그대로, 트래픽 처리량에 따라 ***Pod의 "갯수"*** 를 자동으로 조절한다.


  <img width="1066" height="665" alt="image" src="https://github.com/user-attachments/assets/7324fab8-7fab-44e5-bfb3-2c732e832eba" />


  > HPA는 이름에서 처럼 Scale-Out & Scale-In 방식이다. Container가 구현된 이미지의 Spec은 관계없다.


- 지표를 분석해야 하기 때문에, 대상 Pod를 지속 모니터링하고 있는 `Metric Server`가 있어야 한다.
- Metric Server 설치 방법
  - `curl -Ls https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml -o metric.yml` : Manifest 다운로드


    <img width="1102" height="303" alt="image" src="https://github.com/user-attachments/assets/aa6ba0ba-7c71-425c-b37a-57bb0a18392c" />


  - `kubectl apply -f metric.yml` : Metric Server 역할을 하는 Pod 실행


- 모니터링 대상이 될 Service(Deployment 포함) `yaml` 파일 예시
  ```
  apiVersion: v1
  kind: Service
  metadata:
    name: my-svc
  spec:
    selector:
      app: my-web
    ports:
    - port: 80
      targetPort: 80
  ---
  apiVersion: apps/v1
  kind: Deployment 
  metadata:
    name: my-dep
  spec:
    replicas: 3
    selector:
      matchLabels:
        app: my-web
    template:
      metadata:
        labels:
          app: my-web
      spec:
        containers:
        - image: 61.254.18.30:5000/ipnginx
          name: my-web-con                  # 여기까진 Service ~ Deployment로 이어지는 일반적인 Manifest 
          resources:                        # Metric을 제공해야 하므로, 오브젝트 생성 시 요청할 자원을 선언한다.
            requests:                       # 생성 시 요청할 CPU 사용량, 메모리 용량
              cpu: "10m"
              memory: "64Mi" 
            limits:                         # 할당할 최대 CPU 사용량, 메모리 용량
              cpu: "20m"
              memory: "128Mi"
  ```

- HPA `yaml` 파일 예시
  ```
  apiVersion: autoscaling/v1
  kind: HorizontalPodAutoscaler                    # 여기서 HPA로 선택한다.
  metadata:
    name: hpa-nginx
  spec:
    maxReplicas: 5                                 # 최대 복제본 갯수
    minReplicas: 1                                 # 최소 복제본 갯수
    scaleTargetRef:                                # 타겟이 되는 걸 지정할텐데
      apiVersion: apps/v1                     
      kind: Deployment                             
      name: my-dep                                 # Deployment의 이름을 매핑한다!!
    targetCPUUtilizationPercentage: 50             # CPU 사용량 임계치를 50%로 한다.
  ```
  - `Deployment`의 Manifest에 `replicas: ` 값이 들어가있더라도, `HPA` Manifest의 min~max Replica값을 자동으로 따라간다!
    > 처음에 복제본 만들어 놓더라도 트래픽 없으면 min으로 줄어듬.


## 2. HAproxy
- 3개의 Master Node가 있을 경우, ***1개의 Node만 Active, 2개는 Standby 상태로 구성***한다.
- Active Node가 문제 생기면 Standby Node가 Fail-Over되오 Active 상태로 전환된다.
- #### 새로운 Master Node를 선출하기 위해서는 Master Node의 갯수를 홀수개(3,5,7,9....)로 가져가야 한다!


  <img width="1122" height="598" alt="image" src="https://github.com/user-attachments/assets/0ae7aced-9202-4010-9df0-7597b60bae4c" />


  ### (1) HAproxy Server 구성
    - ```apt update -y && apt install -y haproxy``` : HAproxy 패키지 설치
    - ```vi /etc/haproxy/haproxy.cfg``` : HAproxy 컨피그 파일 편집

        ```
        frontend kubernetes-master-lb
        bind 0.0.0.0:6443
        option tcplog
        mode tcp

        default_backend kubernetes-master-nodes

        backend kubernetes-master-nodes
        mode tcp
        balance roundrobin
        option tcp-check
        option tcplog
        server k8s-master1 <첫번째 Master Node의 IP주소>:6443 check
        server k8s-master2 <두번째 Master Node의 IP주소>:6443 check
        server k8s-master3 <첫번째 Master Node의 IP주소>:6443 check

        ※ 이 예시에서는 Master Node가 3대인 상태로 구성했다. 갯수 변경 시 맞춰서 구성하며 뇐다.
        ```
    - ```systemctl enable --now haproxy``` : HAproxy 데몬 활성화
      
      
  ### (2) 클러스터 이니셜라이징
    - 반드시 Master Node 1번에서만 수행해야한다!
      ```
      kubeadm init --pod-network-cidr=10.244.0.0/16 --upload-certs \
      --kubernetes-version=v1.30.3  --ignore-preflight-errors=all --ignore-preflight-errors=all \
      --control-plane-endpoint=<HAproxy Server의 IP 주소>
      ```
    - 기존 K8s Master Node 초기화 명령과는 다르게, ***★kubectl 주소를 HAproxy 주소로 설정★***한다!
    - 이후로는 동일하지만, 다른 Master Node를 위한 `kubeadm join 명령어`를 확인하여 나머지 Master Node에 적용한다.
    - 다만, 루트 디렉토리에 ***config 구성은 Master Node 1번에서만 수행***하면 된다!


  ### (3) HAproxy Server ~ 클러스터 연동
    - `kubectl` 명령 활성화
      ```
      apt-get install -y apt-transport-https ca-certificates curl
      mkdir -p /etc/apt/keyrings
      curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.30/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
      echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.30/deb/ /' | sudo tee /etc/apt/sources.list.d/kubernetes.list
      apt-get update
      apt-get install -y kubectl
      apt-mark hold kubectl

      ※ 이전에 했던 K8s 세팅이랑 완전 동일하다.
      ```
    - Master-Node 1번의 `/etc/kubernetes/admin.conf` 파일 복사
    - HAproxy 서버에서 `mkdir ~/.kube`, `vi ~/.kube/config` 로 붙여넣으면 세팅 완료


## 3. Node Selector
- K8s에서 ***"스케줄링"을 단순화***하기 위해 도입된 메커니즘
- Pod가 `<key>:<value>` 형태의 어떤 라벨(`Label`)을 갖추고 있는 Node에서만 동작하도록 필터링한다.
- `kubectl label node <Worker Node의 Host 이름> <지정할 label>` : Node에 Label 생성 및 지정(예: gpu=false)
- Pod(Deployment)의 Manifest 파일 內 `spec: ` 하위 항목에 `nodeSelector: { key: value }`를 추가하여 label을 붙여준다.
- 오직 ***라벨과의 “정확한 일치”만 조건***으로 사용 가능하다! AND,OR 등은 구현 불가능하다.
- 대표적인 활용 가능한 상황
  - **특정 노드에만 설치되어 있는 하드웨어를 사용해야 하는 경우** (예: GPU, SSD, 전용 NIC 등)
  - **워크로드 테스트**를 위해 Node를 구분하고 싶을 때 (예: 개발용, QA용, 프로덕션용 등)
  - **원초적으로 단순화된 스케줄링**이 필요한 환경 (예: 소규모 클러스터에서 ‘이 노드에만 올려야 해!’처럼 명시가 필요한 경우)


## 4. Node Affinity
-  Pod가 특정 노드의 라벨(label)이나 속성을 기준으로 배치되는 방식을 세밀하게 조정하여 유연성을 챙긴 메커니즘
  > Affinity = 친밀도, 인력


- **"이런 라벨이 달린 노드에 스케줄링되었으면 좋겠다"** 하는 의미.
- Manifest `yaml` 파일을 바탕으로, API Server가 `spec: ` 정보를 `etcd`에 저장한다.
- 그 이후, Scheduler가 스케줄링 과정에서 Pod의 NodeAffinity 설정을 확인하고 조건에 맞는 Node를 찾아서 Pod를 배치한다.
- NodeAffinity는 특정 노드 라벨이 있는 노드에 ***우선적으로 또는 반드시 배치하도록 설정***할 수 있다.


### ※ 중요 규칙


(1) `requiredDuringSchedulingIgnoredDuringExecution` : "필요조건"
  - Pod를 스케줄링할 때(`DuringScheduling`) 해당 조건이 “반드시(`required`)” 만족되는 노드에만 스케줄링한다.
  - 그런데, Pod가 실행중(`Execution`)인 동안에는 무시(`Ignored`)된다.
    > 쉽게 말해서 K8s의 Scheduler가 Pod를 처음 스케줄링할 때 특정 조건을 반드시 충족해야 하고, 다른 애를 쫓아내진 않는다.
  
  
(2) `preferredDuringSchedulingIgnoredDuringExecution` : "선호조건"
  - Pod를 스케줄링할 때(`DuringScheduling`) 해당 조건이 “가능하면(`preferred`)” 충족하는 노드에 스케줄링하려고 한다.
  - 그런데, Pod가 실행중(`Execution`)인 동안에는 무시(`Ignored`)된다.
    > 쉽게 말해서 K8s의 Scheduler가 Pod를 처음 스케줄링할 때 특정 조건(노드 라벨 등)을 선호하지만, 꼭 지킬 필요는 없다.


- 대표적인 활용 가능한 상황
  - **특정 하드웨어 스펙 노드에만 배치하고 싶을 때** (예: GPU 달린 노드에서만 실행되어야 하는 ML/딥러닝, DB 워크로드)
  - **지역성(Region/Zone)에 따라 배포하고 싶을 때** (예: AWS의 여러 AZ에 분산 배치해서 고가용성 고려)
  - **특정 노드와 충돌을 피하고 싶을 때** (예: 리소스가 제한된 노드에는 중요 워크로드를 배치하지 않으려고 할 때)


- Node Seletor와 마찬가지로, 각 Worker Node에 `kubectl label nodes` 명령을 통해 라벨을 붙여줘야 한다.
- `yaml` 파일 예시
  ```
  apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: my-aff
  spec:
    replicas: 4
    selector:
      matchLabels:
        app: my-httpd
    template:
      metadata:
        labels:
          app: my-httpd
      spec:
        containers: 
          - name: httpd-pod-con
            image: 61.254.18.30:5000/ipnginx
  
        affinity:                    # containers: 와 동등한 레벨에서 작성 시작!!
          nodeAffinity:

            ※ preferred 예시
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:          # perference이기 때문에 하나 더 조건을 부여.  
                  matchExpressions:
                    - key: gpu
                      operator: In
                      values:
                        - 'false'

            ※ required 예시
            requiredDuringSchedulingIgnoredDuringExecution:
              nodeSelectorTerms:     # 아래 조건을 만족하는 노드에만 스케줄링한다. (Selector 기법 차용)
              - matchExpressions:
                - key: gpu
                  operator: In
                  values:
                  - "false"
  ```
- `weight` 점수를 다르게 부여하여 우선 순위를 매길 수 있다.
- 일반적인 Pod보단 ***Deployment, StatefulSet과 함께 적용***하여 실무에서 많이 쓰인다.


## 5. Taint & Tolerations  


## 6. Helm 이란?

## 🔥 도전 과제 & 해결 (Challenge & Solution)

- **과제/문제**: X
- **시도 및 해결**: X

## 💭 오늘의 회고 (Retrospective)

- **Keep (좋았던 점)**: 따라가는데 어려움이 없었다!
- **Problem (아쉬웠던 점)**: 저녁 시간에 이제 공부 시간을 더 할애해야한다.
- **Try (시도할 점)**: CCNA 강의 마무리 해야한다.

## 🔗 참고 자료 (References)
