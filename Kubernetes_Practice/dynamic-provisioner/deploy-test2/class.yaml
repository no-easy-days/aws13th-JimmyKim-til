---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: jimmy-storageclass-fast
#provisioner: k8s-sigs.io/nfs-subdir-external-provisioner # or choose another name, must match deployment's env PROVISIONER_NAME'
provisioner: jimmy-dynamicprovisioner-fast
parameters:
  archiveOnDelete: "false"

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: jimmy-storageclass-slow
#provisioner: k8s-sigs.io/nfs-subdir-external-provisioner # or choose another name, must match deployment's env PROVISIONER_NAME'
provisioner: jimmy-dynamicprovisioner-slow
parameters:
  archiveOnDelete: "false"

